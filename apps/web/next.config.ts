import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Phase 0: minimal config — features added per phase.
  // Static export option available if hosting is static-only (per tech stack notes).
  reactStrictMode: true,
};

export default nextConfig;
