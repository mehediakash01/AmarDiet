// @thali/domain — Phase 0 placeholder
// Pure business rules with zero framework dependencies (no React, no Fastify).
// Domain logic is added in Phase 1+ per module (nutrition, food, diet-plan, etc.).

export {};
